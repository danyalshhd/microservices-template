apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-data-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-data-mongo
  template:
    metadata:
      labels:
        app: user-data-mongo
    spec:
      containers:
        - name: user-data-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: user-data-mongo-srv
spec:
  selector:
    app: user-data-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
